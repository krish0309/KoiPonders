# Copilot Instructions

## Project Guidelines
- When editing files in the KoiPonders workspace, write changes directly to disk via terminal commands (e.g., PowerShell Get-Content/Set-Content) instead of relying on the in-editor file edit tool, because the edit tool's changes were not landing on disk.